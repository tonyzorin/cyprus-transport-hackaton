version: "3.8"

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: bus-hackaton-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-hackaton}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-hackaton_password}
      POSTGRES_DB: ${DB_DATABASE:-bus_hackaton}
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-hackaton} -d ${DB_DATABASE:-bus_hackaton}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Python FastAPI Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: bus-hackaton-backend
    restart: unless-stopped
    environment:
      - DB_USER=${DB_USER:-hackaton}
      - DB_PASSWORD=${DB_PASSWORD:-hackaton_password}
      - DB_DATABASE=${DB_DATABASE:-bus_hackaton}
      - DB_HOST=postgres
      - DB_PORT=5432
    volumes:
      - ./backend/app:/app/app
      - ./backend/uploads:/app/uploads
      - ./gtfs_data:/app/gtfs_data
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy

  # Next.js Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: bus-hackaton-frontend
    restart: unless-stopped
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:${BACKEND_PORT:-8000}
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    ports:
      - "${FRONTEND_PORT:-3001}:3000"
    depends_on:
      - backend

networks:
  default:
    name: bus-hackaton-network
